# Syntax

### `where` clause

Use  `!` to negate.

Note that csh shells (bash, zsh and others) use ! for history expansion,
but it can be prevented with single quotes (making it literal),
e.g. `fm select from * where '!url'`.
